import std.stdio, std.string,std.range, std.conv, std.array, std.algorithm, std.math, std.typecons;

void main() {
  auto win1 = readln.split.to!(int[]);
  auto win2 = readln.split.to!(int[]);

  alias f = {
    if (win1[0] == win2[0]) return true;
    if (win1[0] == win2[1]) return true;
    if (win1[1] == win2[0]) return true;
    if (win1[1] == win2[1]) return true;
    return false;
  };
  writeln(f ? "YES" : "NO");
}
